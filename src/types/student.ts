export interface StudentInfo {
  name: string;
  matricula: string;
  program?: string;
  faculty?: string;
  plan?: string;
}
export interface StudentStatus {
  label: string;
  rawText: string;
  updatedAt: string;
}
